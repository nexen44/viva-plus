kind = "web"
previewPath = "/"
title = "Viva+"
version = "1.0.0"
id = "artifacts/viva-plus"
router = "path"

[[services]]
name = "web"
paths = ["/"]
localPort = 5000

[services.env]
PORT = "5000"

[services.production]
run = ["sh", "-c", "cd artifacts/viva-plus && node server.js"]