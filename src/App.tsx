import { useEffect, useState } from "react"; import { supabase } from "./lib/supabase"; import { StoreCatalog } from "./components/StoreCatalog"; import { PaymentCheckout } from "./components/PaymentCheckout"; export function App() { const [session, setSession] = useState<any>(null); const [loading, setLoading] = useState(true); const [activeTab, setActiveTab] = useState<"home" | "dashboard" | "store" | "community" | "payment" | "admin">("home"); const [userRole, setUserRole] = useState<string>("user"); useEffect(() => { supabase.auth.getSession().then(({ data: { session } }) => { setSession(session); if (session?.user) { supabase.from("profiles").select("role").eq("id", session.user.id).single().then(({ data }) => { if (data?.role) setUserRole(data.role); }); } setLoading(false); }); const { data: { subscription } } = supabase.auth.onAuthStateChange((_event, session) => { setSession(session); }); return () => subscription.unsubscribe(); }, []); if (loading) { return ( <div className="min-h-screen flex items-center justify-center bg-gray-100"> <p className="text-gray-600 font-medium">Carregando Viva+...</p> </div> ); } return ( <div className="min-h-screen bg-gray-100 flex flex-col font-sans"> <header className="bg-white shadow-sm border-b"> <div className="max-w-6xl mx-auto px-6 py-4 flex flex-wrap justify-between items-center gap-4"> <div className="flex items-center space-x-6"> <h1 className="text-2xl font-bold text-blue-600">VIVA+</h1> <nav className="flex space-x-2 text-sm font-medium"> <button onClick={() => setActiveTab("home")} className={`px-3 py-1.5 rounded-md ${activeTab === "home" ? "bg-blue-100 text-blue-700" : "text-gray-600 hover:bg-gray-100"}`}>Home</button> <button onClick={() => setActiveTab("dashboard")} className={`px-3 py-1.5 rounded-md ${activeTab === "dashboard" ? "bg-blue-100 text-blue-700" : "text-gray-600 hover:bg-gray-100"}`}>Dashboard</button> <button onClick={() => setActiveTab("store")} className={`px-3 py-1.5 rounded-md ${activeTab === "store" ? "bg-blue-100 text-blue-700" : "text-gray-600 hover:bg-gray-100"}`}>Loja</button> <button onClick={() => setActiveTab("community")} className={`px-3 py-1.5 rounded-md ${activeTab === "community" ? "bg-blue-100 text-blue-700" : "text-gray-600 hover:bg-gray-100"}`}>Comunidade</button> <button onClick={() => setActiveTab("payment")} className={`px-3 py-1.5 rounded-md ${activeTab === "payment" ? "bg-blue-100 text-blue-700" : "text-gray-600 hover:bg-gray-100"}`}>Assinaturas</button> {userRole === "admin" && ( <button onClick={() => setActiveTab("admin")} className={`px-3 py-1.5 rounded-md ${activeTab === "admin" ? "bg-red-100 text-red-700" : "text-gray-600 hover:bg-gray-100"}`}>Admin</button> )} </nav> </div> <div> {session ? ( <button onClick={() => supabase.auth.signOut()} className="text-sm font-medium text-red-600 hover:underline">Sair</button> ) : ( <span className="text-sm text-gray-500 font-medium">Modo Visitante / Convidado</span> )} </div> </div> </header> <main className="flex-1 max-w-6xl w-full mx-auto p-6"> {activeTab === "home" && ( <div className="bg-white p-6 rounded-lg shadow space-y-4"> <h2 className="text-xl font-bold text-gray-800">Boas-vindas ao VIVA+</h2> <p className="text-gray-600">Autenticação & Status do Sistema</p> {!session ? ( <div className="p-4 bg-blue-50 border border-blue-200 rounded-md"> <p className="text-sm text-blue-800">Faça Login ou Cadastre-se no Supabase para acessar suas métricas personalizadas.</p> </div> ) : ( <p className="text-sm text-green-700 font-medium">Conectado como: {session.user.email}</p> )} </div> )} {activeTab === "dashboard" && ( <div className="bg-white p-6 rounded-lg shadow space-y-4"> <h2 className="text-xl font-bold text-gray-800">Seu Dashboard de Saúde & Hábitos</h2> <p className="text-gray-600">Métricas de hábitos, jornadas e desafios vinculados ao seu perfil.</p> </div> )} {activeTab === "store" && <StoreCatalog />} {activeTab === "community" && ( <div className="bg-white p-6 rounded-lg shadow space-y-4"> <h2 className="text-xl font-bold text-gray-800">Feed da Comunidade</h2> <p className="text-gray-600">Publicações e trocas dos participantes do VIVA+.</p> </div> )} {activeTab === "payment" && <PaymentCheckout />} {activeTab === "admin" && ( <div className="bg-white p-6 rounded-lg shadow space-y-4 border-l-4 border-red-500"> <h2 className="text-xl font-bold text-red-600">Painel Administrativo</h2> <p className="text-gray-600">Gestão de usuários, produtos da loja e posts da comunidade.</p> </div> )} </main> </div> ); } export default App;
